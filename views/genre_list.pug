extends layout 

block content 
    h1=title 

    if genre_list.length 
        ul 
            each value in genre_list 
                li  
                    a(href=value.url) #{value.name}
    else 
        p There are no genres.