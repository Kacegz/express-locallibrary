extends layout

block content

  h1 #{title}: #{instance.imprint}

    p Do you really want to delete this instance?

    form(method='POST')
      div.form-group
        input#instanceid.form-control(type='hidden', name='id', value=instance._id )

      button.btn.btn-primary(type='submit') Delete